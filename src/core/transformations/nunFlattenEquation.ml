
(* This file is free software, part of nunchaku. See file "license" for more details. *)

(** {1 Flatten pattern-matching in Equations} *)

module Make(T : NunTerm_ho.S) = struct

    let pipe ~print = assert false

end
